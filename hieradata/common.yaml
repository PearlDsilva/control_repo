classes-redhat:
  - firewalld
  - sudo 
  - sudo::configs
  - package
  
package::package:
  wget:
    ensure: latest
    
firewalld::ports:
  'Rubrik RBS 12800':
    ensure: present
    zone: public
    port: 12800
    protocol: 'tcp' 
