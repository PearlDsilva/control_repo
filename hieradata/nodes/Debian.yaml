classes-debian:
        - package

package::package:
        vim:
                ensure: present

firewalld::ports:
  'Open port 80 for jenkins console':
    ensure: present
    zone: public
    port: 80
    protocol: 'tcp'
  'Open port 443 for jenkins console':
    ensure: present
    zone: public
    port: 443
    protocol: 'tcp'
