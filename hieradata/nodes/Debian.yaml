classes-debian:
        - package

package::package:
        vim:
                ensure: present

firewalld::ports:
  'Open port 80 for jenkins console':
    ensure: present
    zone: public
    port: 80
    protocol: 'tcp'
  'Open port 443 for jenkins console':
    ensure: present
    zone: public
    port: 443
    protocol: 'tcp'
  'Open port 12803  for testing':
    ensure: present
    zone: public
    port: 12803
    protocol: 'tcp'    
