---
version: 5

defaults:
  datadir: hieradata          # Datadir has moved into `defaults`.
  data_hash: yaml_data   # Default backend: New feature in v5.

hierarchy:
  - name: "Nodes"
    paths:
      - "nodes/Debian.yaml"
      - "nodes/%{::osfamily}.yaml"
      - "common.yaml"

